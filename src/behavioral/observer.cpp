#include "behavioral/observer.hpp"

#include <iostream>
#include <unordered_set>

void pattern::behavioral::observer::Run() {
//	Observer a{};
//	Observer b{};
//	a.val = 66;
//	b.val = 33;
//	std::unique_ptr<Observer> a_ptr{std::make_unique<Observer>(a)};
//	std::unique_ptr<Observer> b_ptr{std::make_unique<Observer>(b)};
//	std::unordered_set<std::unique_ptr<Observer>> my_set{};
//	my_set.insert(a_ptr);
//	my_set.insert(b_ptr);
//
//	std::cout << "a = " << my_set.find(a_ptr)->get()->val << " b = " << my_set.find(b_ptr)->get()->val;
//
//		//std::cout << __FILE__ << " " << __FUNCTION__ << " add_result: " << client.add_result;
}
